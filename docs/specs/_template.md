# Feature spec: <feature name>

> Copy this template to `docs/specs/<feature>.md` and fill in the
> sections. Delete section bodies that don't apply, but keep the
> headings — they remind us what to think about.

## User story

As a `<user role>`, I want `<capability>`, so that `<value>`.

## Preconditions

- ...

## Main flow

1. ...

## Edge cases

- ...

## Out of scope

- ...

## Design

### Domain model

New types: `<list>`.

### Module layout

Where each piece lives.

### Seams (test surface)

What can be tested in isolation?

### Performance

Specific concerns and approach.

## Localization keys

- `feature.button.label`
- `feature.error.message`

## Test plan

- [ ] Unit: `<test name>` — `<file>`
- [ ] UI: `<test name>` — `<file>`
- [ ] Localization: every visible string has a translation key

## Acceptance criteria

- [ ] ...

## Open questions

- ...

## References

- Code: `<path>`
- Spec: `<path or URL>`
